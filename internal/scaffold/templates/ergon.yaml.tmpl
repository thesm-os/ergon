# ergon configuration for {{.Name}}. Every field is optional;
# values not set here fall back to the subsystem's Defaults().

name: {{.Name}}

# modules: []
# bootstrap: {extra_tools: []}
# license: {config_file: .go-license.yml}
# markdown: {globs: ["**/*.md", "#vendor", "#dist", "#node_modules"]}
# test: {cpu: 4, count: 3, timeout: 10m, race_count: 3, bench_count: 6, fuzz_time: 30s}
# bench: {baseline_path: bench/baseline.txt}
# checks:
#   coverage: {packages: [], excludes: []}
#   mutation: {packages: []}
#   error_prefix: {target_dirs: ["."]}
#   commit_msg: {types: [feat, fix, docs, refactor, test, ci, chore, perf, build, revert], max_subject_length: 72}
